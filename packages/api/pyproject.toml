[project]
name = "codeops-api"
version = "0.1.0"
description = "CodeOps API Gateway"
dependencies = [
    "codeops-core",
    "fastapi>=0.100.0",
    "uvicorn>=0.20.0",
    "celery>=5.3.0",
    "redis>=5.0.0",
]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
