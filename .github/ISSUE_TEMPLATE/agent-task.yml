# .github/ISSUE_TEMPLATE/agent-task.yml
# ══════════════════════════════════════════════════════════════════════════════
# 🤖 AI Agent Task Template - SOTA Configuration
# ══════════════════════════════════════════════════════════════════════════════

name: "🤖 Agent Task"
description: "Create a new task for an AI coding agent"
title: "[Agent]: "
labels: ["agent-task", "triage", "awaiting-assignment"]
assignees: []

body:
  # ──────────────────────────────────────────────────────────────────────────────
  # 📋 HEADER SECTION
  # ──────────────────────────────────────────────────────────────────────────────

  - type: markdown
    attributes:
      value: |
        ## 🤖 AI Agent Task Request

        Complete this form to create a well-structured task for an AI coding agent.
        The more context you provide, the better the agent can execute the task.

        ---

  # ──────────────────────────────────────────────────────────────────────────────
  # 🎯 CORE TASK DEFINITION
  # ──────────────────────────────────────────────────────────────────────────────

  - type: dropdown
    id: agent
    attributes:
      label: "🤖 Assigned Agent"
      description: "Select the AI agent best suited for this task"
      options:
        - "🔮 Auto-Assign (Let maintainers decide)"
        - "── Claude Family ──"
        - "ClaudeCode (Precise coding, documentation)"
        - "Composer (Real-time editing, IDE integration)"
        - "── Google Family ──"
        - "GeminiFlash25 (Gemini CLI Flash 2.5)"
        - "GeminiPro25 (Gemini CLI Pro 2.5)"
        - "GeminiPro30 (Gemini Pro 3.0)"
        - "Jules (CI/CD automation, DevOps)"
        - "── OpenAI Family ──"
        - "Codex (OpenAI)"
        - "GitHub Copilot"
        - "── xAI Family ──"
        - "GrokIA (Contextual analysis, intelligent refactoring)"
        - "Grok 3"
        - "── Other Agents ──"
        - "Windsurf (Codeium)"
        - "Aider"
        - "Devin (Cognition)"
        - "Amazon Q Developer"
        - "Cody (Sourcegraph)"
        - "Tabnine"
        - "Replit Ghostwriter"
        - "Antigravity"
        - "── Custom/Other ──"
        - "Custom Agent (specify below)"
    validations:
      required: true

  - type: input
    id: custom_agent
    attributes:
      label: "Custom Agent Name"
      description: "If you selected 'Custom Agent' above, specify the agent name"
      placeholder: "e.g., MyCustomAgent v2.1"
    validations:
      required: false

  - type: dropdown
    id: task_type
    attributes:
      label: "📌 Task Type"
      description: "Primary type of work required"
      options:
        - "🆕 CREATE - Build new feature/component"
        - "♻️ REFACTOR - Improve existing code structure"
        - "🐛 DEBUG - Find and fix bugs"
        - "⚡ OPTIMIZE - Improve performance"
        - "📝 DOCUMENT - Write/update documentation"
        - "🔍 ANALYZE - Code review/analysis"
        - "🧪 TEST - Write/improve tests"
        - "🔧 CONFIGURE - Setup/configuration changes"
        - "🔗 INTEGRATE - Third-party integration"
        - "🔒 SECURITY - Security improvements"
        - "🗃️ DATABASE - Schema/migration changes"
        - "🎨 UI/UX - Frontend/styling changes"
        - "🏗️ INFRASTRUCTURE - DevOps/CI/CD changes"
        - "📦 DEPENDENCY - Update/manage dependencies"
        - "🧹 CLEANUP - Remove dead code/tech debt"
    validations:
      required: true

  - type: dropdown
    id: priority
    attributes:
      label: "🚨 Priority Level"
      description: "How urgent is this task?"
      options:
        - "🔴 P0 - Critical (Production blocker)"
        - "🟠 P1 - High (Needed this sprint)"
        - "🟡 P2 - Medium (Planned work)"
        - "🟢 P3 - Low (Nice to have)"
        - "⚪ P4 - Backlog (Future consideration)"
      default: 2
    validations:
      required: true

  - type: dropdown
    id: complexity
    attributes:
      label: "📊 Estimated Complexity"
      description: "How complex is this task?"
      options:
        - "🟢 Trivial (< 30 min, single file change)"
        - "🟡 Simple (< 2 hours, few files)"
        - "🟠 Medium (< 1 day, multiple components)"
        - "🔴 Complex (< 1 week, architectural changes)"
        - "⚫ Epic (> 1 week, major feature)"
      default: 1
    validations:
      required: true

  # ──────────────────────────────────────────────────────────────────────────────
  # 📝 TASK DETAILS
  # ──────────────────────────────────────────────────────────────────────────────

  - type: markdown
    attributes:
      value: |
        ---
        ## 📝 Task Details

  - type: textarea
    id: description
    attributes:
      label: "📋 Task Description"
      description: "Provide a clear, detailed description of what needs to be done"
      placeholder: |
        ## Objective
        Create a new authentication middleware that...

        ## Background
        Currently, we handle auth in each route handler, which leads to...

        ## Expected Outcome
        A reusable middleware that...
      value: |
        ## Objective
        <!-- What should be accomplished? -->

        ## Background
        <!-- Why is this needed? What problem does it solve? -->

        ## Expected Outcome
        <!-- What does success look like? -->
    validations:
      required: true

  - type: textarea
    id: acceptance_criteria
    attributes:
      label: "✅ Acceptance Criteria"
      description: "Define specific, testable requirements for task completion"
      placeholder: |
        - [ ] Middleware validates JWT tokens correctly
        - [ ] Returns 401 for invalid/expired tokens
        - [ ] Attaches user object to request
        - [ ] Unit tests with >80% coverage
        - [ ] Documentation updated
      value: |
        - [ ]
        - [ ]
        - [ ]
        - [ ] Tests passing
        - [ ] No linting errors
    validations:
      required: true

  # ──────────────────────────────────────────────────────────────────────────────
  # 📁 CONTEXT & SCOPE
  # ──────────────────────────────────────────────────────────────────────────────

  - type: markdown
    attributes:
      value: |
        ---
        ## 📁 Context & Scope

  - type: textarea
    id: target_files
    attributes:
      label: "📂 Target Files/Directories"
      description: "Specify the files or directories the agent should focus on"
      placeholder: |
        ### Primary Files
        - `src/middleware/auth.ts` (create)
        - `src/routes/api/*.ts` (modify)

        ### Related Files (for context)
        - `src/types/user.ts`
        - `src/config/jwt.ts`
      render: markdown
    validations:
      required: false

  - type: textarea
    id: context_constraints
    attributes:
      label: "🔒 Context & Constraints"
      description: "Technical requirements, limitations, or specific guidelines"
      placeholder: |
        ### Tech Stack
        - Node.js 20+, TypeScript 5.x
        - Express.js framework
        - Jest for testing

        ### Constraints
        - Must be backward compatible
        - No new dependencies without approval
        - Follow existing code style (Prettier + ESLint)

        ### Security Requirements
        - Tokens must be validated server-side
        - No sensitive data in logs
    validations:
      required: false

  - type: textarea
    id: references
    attributes:
      label: "🔗 References & Resources"
      description: "Links to docs, examples, related issues, or PRs"
      placeholder: |
        - [JWT Best Practices](https://example.com/jwt-guide)
        - Related issue: #123
        - Similar implementation: `src/middleware/rateLimit.ts`
        - Design doc: [Link to Notion/Confluence]
    validations:
      required: false

  # ──────────────────────────────────────────────────────────────────────────────
  # ⚙️ EXECUTION SETTINGS
  # ──────────────────────────────────────────────────────────────────────────────

  - type: markdown
    attributes:
      value: |
        ---
        ## ⚙️ Execution Settings

  - type: input
    id: branch_name
    attributes:
      label: "🌿 Suggested Branch Name"
      description: "Recommended branch name for this task. Follow format: agent/{AgentName}/{feature-description}"
      placeholder: "agent/ClaudeCode/feat-auth-middleware"
    validations:
      required: false

  - type: dropdown
    id: base_branch
    attributes:
      label: "🎯 Base Branch"
      description: "Branch to base the work on"
      options:
        - "main"
        - "master"
        - "develop"
        - "staging"
        - "Other (specify in context)"
      default: 0
    validations:
      required: true

  - type: checkboxes
    id: requirements
    attributes:
      label: "📋 Task Requirements"
      description: "Select all that apply to this task"
      options:
        - label: "🧪 Requires unit tests"
          required: false
        - label: "🔬 Requires integration tests"
          required: false
        - label: "📝 Requires documentation updates"
          required: false
        - label: "🔄 Requires database migration"
          required: false
        - label: "⚠️ Contains breaking changes"
          required: false
        - label: "🔒 Requires security review"
          required: false
        - label: "👀 Requires manual QA"
          required: false
        - label: "📊 Requires performance benchmarks"
          required: false

  - type: dropdown
    id: auto_merge
    attributes:
      label: "🔀 Auto-Merge Preference"
      description: "Should the resulting PR be auto-merged if checks pass?"
      options:
        - "❌ No - Requires manual review"
        - "⚠️ Squash merge if checks pass"
        - "✅ Auto-merge if checks pass"
      default: 0
    validations:
      required: true

  # ──────────────────────────────────────────────────────────────────────────────
  # 🔗 DEPENDENCIES
  # ──────────────────────────────────────────────────────────────────────────────

  - type: markdown
    attributes:
      value: |
        ---
        ## 🔗 Dependencies & Relationships

  - type: input
    id: blocked_by
    attributes:
      label: "🚫 Blocked By"
      description: "Issues that must be completed before this task"
      placeholder: "#101, #102"
    validations:
      required: false

  - type: input
    id: blocks
    attributes:
      label: "⏳ Blocks"
      description: "Issues that are waiting on this task"
      placeholder: "#201, #202"
    validations:
      required: false

  - type: input
    id: related_issues
    attributes:
      label: "🔗 Related Issues"
      description: "Other related issues or PRs"
      placeholder: "#301, #302, PR #401"
    validations:
      required: false

  # ──────────────────────────────────────────────────────────────────────────────
  # 📊 METADATA
  # ──────────────────────────────────────────────────────────────────────────────

  - type: markdown
    attributes:
      value: |
        ---
        ## 📊 Additional Metadata

  - type: dropdown
    id: component
    attributes:
      label: "🧩 Component/Area"
      description: "Which part of the system does this affect?"
      multiple: true
      options:
        - "Backend / API"
        - "Frontend / UI"
        - "Database"
        - "Authentication"
        - "Authorization"
        - "Infrastructure"
        - "CI/CD"
        - "Documentation"
        - "Testing"
        - "DevOps"
        - "Mobile"
        - "CLI"
        - "SDK / Library"
        - "Other"
    validations:
      required: false

  - type: dropdown
    id: languages
    attributes:
      label: "💻 Programming Languages"
      description: "Primary languages involved"
      multiple: true
      options:
        - "TypeScript"
        - "JavaScript"
        - "Python"
        - "Go"
        - "Rust"
        - "Java"
        - "Kotlin"
        - "C#"
        - "Ruby"
        - "PHP"
        - "Swift"
        - "Dart"
        - "SQL"
        - "Shell/Bash"
        - "YAML/JSON"
        - "Other"
    validations:
      required: false

  - type: textarea
    id: additional_notes
    attributes:
      label: "📝 Additional Notes"
      description: "Any other information that might help the agent"
      placeholder: |
        - The existing auth system uses Passport.js
        - We're planning to migrate to a different solution in Q3
        - Performance is critical for this endpoint
    validations:
      required: false

  # ──────────────────────────────────────────────────────────────────────────────
  # ✅ CONFIRMATION
  # ──────────────────────────────────────────────────────────────────────────────

  - type: markdown
    attributes:
      value: |
        ---
        ## ✅ Confirmation

  - type: checkboxes
    id: confirmation
    attributes:
      label: "Pre-submission Checklist"
      description: "Please confirm the following before submitting"
      options:
        - label: "I have searched for existing issues to avoid duplicates"
          required: true
        - label: "I have provided sufficient context for the agent to understand the task"
          required: true
        - label: "I have defined clear acceptance criteria"
          required: true
        - label: "I have specified the correct base branch"
          required: true
