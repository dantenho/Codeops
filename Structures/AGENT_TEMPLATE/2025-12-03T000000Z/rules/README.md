# rules

Capture protocol excerpts, policy deltas, and any temporary constraints that apply to the agent.
- Include references back to `Agents.MD` sections when copying snippets.
- Version-date each rule document for traceability.
