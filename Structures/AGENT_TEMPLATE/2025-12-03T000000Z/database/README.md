# database

Use this folder for lightweight state captured during training (SQLite snapshots, JSON stores, embeddings).
- Add a short manifest describing schema versions and how to regenerate the data.
