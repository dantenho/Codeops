[project]
name = "codeops-memory"
version = "0.1.0"
description = "Memory and Persistence Layer"
dependencies = ["codeops-core", "chromadb>=0.4.0", "sqlmodel>=0.0.14"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
